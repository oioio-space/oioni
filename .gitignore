# Dolt database files (added by bd init)
.dolt/
*.db

# Gokrazy build artifacts
oioio/builddir/
# config.json et wifi.json sont trackés avec des placeholders.
# Modifications locales (vrais mots de passe) masquées via skip-worktree — voir Makefile.
*.gaf

# Compiled binaries (keep mkfs.* and *.ko — they're pre-built for offline use)
hello/hello
cmd/oioni/oioni

# Go workspace local overrides
go.work.local

# IDE
.idea/
*.iml
